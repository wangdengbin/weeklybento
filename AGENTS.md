# AGENTS.md — 项目级开发规则

> 任何 AI 或开发者在本仓库实现代码（尤其是 UI 与按钮相关改动）之前，**必须先阅读并遵守本文件**。
> 全局通用规则（如始终用中文回复）见用户主目录 `~/.knowledge.md`，本文件为项目级规则，两者叠加生效。

---

## 🎯 核心原则：移动端优先（Mobile-First）

本应用是**面向手机用户的移动端 Web 应用**：
- 默认容器 `#app` 宽度为 **480px**（`src/assets/main.css`），桌面端（≥768px）才加宽到 920px。
- **所有新增 UI 必须先按 375px 手机宽度设计、验收，再考虑桌面端**。桌面端只是"移动端适配后的加宽"，绝不是设计起点。

---

## 📱 按钮与排版铁律（最易违反，务必遵守）

1. **不得挤在一起**：同一行/同一操作区并排的按钮**不超过 2~3 个**；超出必须 `flex-wrap: wrap` 换行，或改为纵向排列，或收纳进"更多"菜单。
2. **触控目标尺寸**：可点击元素高度/最小高度 **≥ 40px**（推荐 44px），相邻按钮间距 **≥ 8px**（推荐 12px）。
3. **加按钮前先数一数**：目标位置现在已有几个按钮/链接？放得下吗？放不下就换行、分组或降级为文字链接，**禁止硬塞导致挤压**。
4. **主次分明**：一个操作区只允许 **1 个主按钮**（渐变 + 阴影），其余必须是次级按钮或文字链接，避免多个高亮按钮视觉争抢。
5. **文案从简**：按钮文字尽量 **≤ 6 个字**（如"确认导入"而非"确认将选中地点全部导入"）；放不下的说明移到副标题、placeholder 或 tooltip。
6. **禁止横向溢出**：新增元素后必须在 375px 宽度下检查是否出现横向滚动条或内容被截断；长文本统一用 `overflow: hidden; text-overflow: ellipsis; white-space: nowrap`。
7. **遵循既有响应式断点**（不要自创新断点）：
   - `≤ 580px`：移动端头部分段胶囊（`HeaderNav.vue` 第二行模式切换）
   - `≤ 640px`：弹窗窄屏布局（各 `*Modal.vue` 的 `@media (max-width: 640px)`）
   - `≥ 768px`：桌面端容器加宽（`#app` 480px → 920px）
   - 新增 UI **必须在移动断点下实测验收**后才能算完成。

---

## 🛠 技术栈与既有约定（实现时保持一致）

- **Vue 3 `<script setup lang="ts">` + Vite**；样式用 scoped CSS，注释用中文。
- **视觉体系**：玻璃拟态——卡片 `background: rgba(255,255,255,.xx)` + `backdrop-filter: blur()`；主色橙系渐变 `#FF6B35` / `#f97316` / `#ea580c`。
- **弹窗结构**：统一 `modal-overlay` + `modal-card glass-modal`；危险/确认操作走 `useToast()` 的 Confirm，**禁止使用原生 `alert`/`confirm`**。
- **图标**：`lucide-vue-next`；轻提示用 `useToast()`（success / error / info 三态）。
- **数据层**：个人数据走 `useBentoStore()`，团队数据走 `useTeamWorkspace()`；云同步 / Realtime 见 `useCloudSync.ts` 与 `useRealtimeSync.ts`（可复用，不要重复造轮子）。
- **提交前必跑**：`npx vue-tsc -b`（类型检查）与 `npx vite build`（构建），两者都通过才算完成。

---

## ✅ 提交前自检清单（UI 相关改动必过）

- [ ] 375px 宽度下无横向溢出、无内容截断
- [ ] 同一区域主按钮 ≤ 1 个，总按钮 ≤ 3 个或已换行 / 分组 / 收纳
- [ ] 触控目标 ≥ 40px，按钮间距 ≥ 8px
- [ ] 移动断点（≤580px / ≤640px）样式已处理并实测
- [ ] 未使用原生 `alert` / `confirm`
- [ ] `npx vue-tsc -b` 与 `npx vite build` 均通过
